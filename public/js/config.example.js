// public/js/config.js 의 예시 파일 — 실제 파일은 git에 커밋되지 않는다(.gitignore).
// 로컬 개발 시 저장소 루트에서 `node generate-config.js` 를 한 번 실행하면
// .env 의 CARTO_API_KEY 값으로 실제 public/js/config.js 가 생성된다.
window.__SAFE_RIDE_ENV__ = {
  CARTO_API_KEY: ''
};
